name: Stage 2 (CI/CD)

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  trivy:
    uses: ./.github/workflows/trivy.yml

  sbom:
    uses: ./.github/workflows/sbom.yml

  build:
    needs: [trivy, sbom]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      - name: Build
        run: echo "Placeholder for build logic"
